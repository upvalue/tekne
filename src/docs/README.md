# docs

the schema of documents and the pure domain code around it: the markdown
parser and its regexes, dependency-free analysis code (e.g. code that finds
table of content from headers, or generates a display-friendly list of
timestamps for the gutters), name/tag validation, and the tutorial document
